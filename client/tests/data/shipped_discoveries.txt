# Stable discovery ids that production has persisted, one per line.
#
# APPEND-ONLY (no-resets law). SaveVault's runtime guard binds this ledger in
# both directions, the boot guard proves every name still maps to a real POI,
# and CI compares this file with the pull request base. Renaming or removing an
# id would make an already-found place look new again to returning players.
starter_cave
wardens_shrine
