# Godot generated caches — never commit.
client/.godot/

# Downloaded bake inputs (pinned + checksummed by bootstrap.sh) — tools and
# source packs never enter the tree, only baked output does.
tools/artgen/humanoid_kit/packs/

# Export artifacts.
build/
*.dmg
*.zip

# OS noise.
.DS_Store

# MegaLinter run output — regenerated locally, never committed.
megalinter-reports/
